import React, { FunctionComponent } from 'react';

interface ILineWorkerProps {
  classname?: string;
}

const LineWorker: FunctionComponent<ILineWorkerProps> = () => {
  return <div>LineWorker</div>;
};

export default LineWorker;
